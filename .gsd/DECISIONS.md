# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | ZK proving strategy | SP1 (Succinct) zkVM for proving the scoring function. Scoring logic written in Rust, compiled to RISC-V, proven by SP1 Hypercube. Output compressed to Groth16 proof for on-chain verification. | SP1 leads in production adoption (6M+ proofs, $3B+ TVL), lowest proving cost ($0.02-0.10/proof), fastest latency (~2 min), formal verification passed, and has exclusive partnerships with OP Stack and Arbitrum. The zkVM model lets us prove arbitrary scoring functions without writing circuit constraints — far more evolvable than a circuit DSL like Noir. | Yes | collaborative |
| D002 |  | architecture | L2 chain for proof verification | Base L2 (Optimism stack). Solidity verifier contract deployed on Base. ~$0.008/proof verification cost. | Base has the lowest verification cost among major L2s ($0.008 vs $0.04 on Arbitrum), EVM-compatible (standard Solidity tooling), large and growing user base, and integrates with SP1 via OP Succinct (SP1 is the default proving system for OP Stack). | Yes | collaborative |
| D003 |  | architecture | Async proving architecture with Ed25519 fallback | Users get their profile immediately with an Ed25519 server attestation. ZK proof generation is async — queued, proven in background, verified on-chain. If ZK proving fails at any step, the Ed25519 attestation remains the valid credential. Profile shows progressive status: pending → scored → proof_generating → on_chain. | The 1-5 min proving latency must not block user experience. Ed25519 attestation is the instant safety net. ZK is an upgrade, not a gate. This pattern keeps the UX fast while building toward trustless verification. | Yes | collaborative |
| D004 |  | architecture | Scoring algorithm iteration model | The scoring algorithm is a living, iterating system — not a build-once spec. Signal weights are role-adaptive and evolve as we learn what matters for different job types. The Rust port for SP1 proving must support weight/parameter updates without full circuit redeployment. | The thesis that all jobs will require coding means the signal mix for a marketing role is fundamentally different than for an engineering role. A static algorithm would miss the point. Iteration based on real usage data is the only way to get this right. | Yes | collaborative |
| D005 |  | architecture | Marketplace pricing model | Individuals always free. Recruiters pay per verification with depth tiers. Gas costs included in tier pricing. Premium tiers get deeper analysis, priority proving, faster turnaround. | Free for candidates drives adoption and data network effects. Recruiter revenue sustains the proving and verification costs. Tiered pricing captures willingness to pay for deeper analysis without limiting the free tier's usefulness. | Yes | collaborative |
| D006 |  | architecture | Deep pipeline data fetching strategy — REST vs GraphQL for README, CI/CD, contribution data | Use GitHub GraphQL API for contribution calendar (contributionsCollection) and REST API for per-repo README content and CI/CD file existence checks. README content via GET /repos/{owner}/{repo}/readme (REST is simpler and returns decoded text). CI/CD detection via GET /repos/{owner}/{repo}/contents/ and checking known CI config paths. Contribution calendar via GraphQL contributionsCollection query batched into 1-year windows. | GraphQL is best for structured queries like contribution calendars where we can specify exact fields. REST is simpler and more reliable for file content retrieval (READMEs, CI/CD configs) since GitHub's GraphQL object() query is more verbose and has size limits. Using both APIs pragmatically minimizes calls and keeps code readable. | Yes | agent |
| D007 |  | architecture | Role-adaptive scoring profile structure | Three built-in profiles (engineering, marketing, non-technical) defined as RoleProfile dataclass instances with distinct 12-signal weights summing to 1.0, per-signal confidence thresholds, and proportional weight redistribution when signals fall below threshold. | D004 established scoring as a living system with role-adaptive weights. The dataclass approach makes profiles introspectable (for API discovery), extensible (add via code), and testable. Confidence thresholding with proportional redistribution preserves profile structure while gracefully handling low-data scenarios. Engineering profile weights kept close to existing defaults for backward compatibility. | Yes | agent |
| D008 |  | architecture | Ed25519 attestation approach for score signing | Use PyNaCl (libsodium/nacl.bindings) for Ed25519 signing. Server generates or loads a signing key from ATTEST_PRIVATE_KEY env var. Score result is serialized as deterministic sorted-key JSON, signed, and the base64 signature+public key+signed payload are returned alongside the score. A /verify endpoint allows third-party verification. | PyNaCl provides a well-audited Ed25519 implementation via libsodium. The deterministic JSON payload (sorted keys) ensures verifiability is not affected by key ordering. Loading the private key from env var supports key rotation without code changes. Auto-generation in dev removes friction. D003 established Ed25519 as the instant attestation path. This approach gives us independently verifiable scores without adding SP1/ZK complexity — exactly the "instant safety net" D003 describes. | Yes | agent |
| D009 | M001/S04 | architecture | GITHUB_TOKEN validation timing | Defer GITHUB_TOKEN validation from module import time to a lazy _get_github_client() function that initializes on first use. The function is called in each endpoint handler instead of referencing a module-level variable. | The module-level raise ValueError prevents pytest from importing api/main.py when GITHUB_TOKEN is unset, making tests brittle. The lazy pattern allows tests to import the module without the env var, while preserving the clear-failure-on-first-use behavior at runtime. Tests monkeypatch 'api.main._get_github_client' to return a mock client. | Yes | agent |
