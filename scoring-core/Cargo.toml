[package]
name = "scoring-core"
version = "0.1.0"
edition = "2021"

[dependencies]
scoring-types = { path = "../scoring-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
